version: 2
jobs:
   build:
     docker:
       - image: circleci/node:latest
     steps:
       - checkout
       - restore_cache:
          key: dependency-appcache-{{ checksum "package.json" }}
       - run: npm install
       - save_cache:
          key: dependency-appcache-{{ checksum "package.json" }}
          paths:
           - ./node_modules
       - run: npm run build
       - run: npm test
       - run: node deploy_app.js

workflows:
  version: 2
  deploy:
    jobs:
      - build:
          filters:
            branches:
              only:
               - master